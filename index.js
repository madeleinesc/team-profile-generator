// packages needed for application
const inquirer = require('inquirer');
const fs = require('fs'); // to read/write files
const jest = require('jest');


// for generate markdown html
const generateHTML = require("./src/generateHTML.js"); 
